# Background music

Current track for scene 12:

**`all-of-the-kittens.mp3`**

Supported formats: MP3 (recommended), or change the path in `src/engine/AudioDirector.ts` if you use `.wav`, `.ogg`, or `.m4a`.

The track plays during scene 12 **The Happy Dance**, starting when the kittens begin dancing, and fades out when the scene ends.
